{% extends 'baseLogin.html.twig' %}

{% block title %} Récupération de mot de passe {% endblock %}

{% block body %}

{{ parent() }}

<div class="container">

{% for message in app.flashes('notice') %}
    <div class="flash-notice">
        {{ message }}<br>
    </div>
{% endfor %}

	<div class="row">
		<div class="col"><p style="text-align: center;" class="h3">Mot de passe perdu</p>
		{{ form_start(pwdLost) }}
		{{ form_row(pwdLost.email)}}
        <button class="btn btn-lg btn-primary" type="submit">Changer mon mot de passe</button>
		{{ form_end(pwdLost)}}
		</div>
	</div>


</div>

{% endblock %}