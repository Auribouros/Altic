{% extends 'baseLogin.html.twig' %}

{% block title %} Altic - Mot de passe oublié {% endblock %}

{% block body %}

{{ parent() }}

<div class="container">
	<div class="row">
		<div class="col"><p style="font-family: 'roboto'; text-align: center;" class="h3">Mot de passe oublié</p>
		{{ form_start(changePassword) }}
		{{ form_row(changePassword.newPassword) }}
        <button class="btn btn-lg btn-primary" type="submit">Changer mon mot de passe</button>
		{{ form_end(changePassword) }}
		</div>
	</div>


</div>
{% endblock %}